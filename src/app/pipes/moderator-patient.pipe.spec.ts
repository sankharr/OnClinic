import { ModeratorPatientPipe } from './moderator-patient.pipe';

describe('ModeratorPatientPipe', () => {
  it('create an instance', () => {
    const pipe = new ModeratorPatientPipe();
    expect(pipe).toBeTruthy();
  });
});
